import { Box } from "@mui/system";
import React from "react";

const Carousel = () => {
  return (
    <Box
      sx={{
        height: "50%",
        display: "flex",
        alignItems: "center"
      }}
    >
      <div>Carousel</div>;
    </Box>
  );
};

export default Carousel;
